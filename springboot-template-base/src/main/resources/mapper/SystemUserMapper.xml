<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.boot.springboottemplatebase.mapper.SystemUserMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.example.boot.springboottemplatebase.domain.systemuser.persistent.SystemUser">
        <id column="id" property="id" />
        <result column="avatar" property="avatar" />
        <result column="create_time" property="createTime" />
        <result column="description" property="description" />
        <result column="last_login_time" property="lastLoginTime" />
        <result column="nickname" property="nickname" />
        <result column="password" property="password" />
        <result column="phone" property="phone" />
        <result column="status" property="status" />
        <result column="update_time" property="updateTime" />
        <result column="username" property="username" />
        <result column="role_id" property="roleId" />
        <result column="create_by" property="createBy" />
        <result column="update_by" property="updateBy" />
        <result column="deleted" property="deleted" />
    </resultMap>

    <select id="securityGetUserByUsername"
            resultType="com.example.boot.springboottemplatebase.domain.systemuser.query.SecurityGetUserByUsernameQO">
        select
            id as user_id,
            username,
            `password`,
            role_id,
            (select name from system_role where id = role_id) as role_name,
            phone,
            nickname,
            avatar,
            description,
            `status`,
            create_time,
            update_time,
            last_login_time
        from
            system_user
        where
            username = #{username}
    </select>

</mapper>
